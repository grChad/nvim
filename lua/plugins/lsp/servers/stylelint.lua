local M = {}

M.settings = {
   stylelintplus = {
      autoFixOnSave = true,
      autoFixOnFormat = true,
      validateOnSave = true,
      -- configFile = vim.fn.expand('~/.config/nvim/static/stylelintrc.json'),
   },
}

return M
