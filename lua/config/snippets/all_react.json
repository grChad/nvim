{
  "module default export function": {
    "prefix": "edfc",
    "body": [
      "export default function ${TM_FILENAME_BASE} (${1}) {",
      "\treturn (${2})",
      "}"
    ]
  },
  "module default export function Arrow": {
    "prefix": "edfa",
    "body": [
      "const ${TM_FILENAME_BASE} = (${1}) => {",
      "\treturn (${2})",
      "}",
      "\nexport default ${TM_FILENAME_BASE}"
    ]
  }
}
