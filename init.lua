-------------------------------------------------------------------------------
--                           by grChad 󰊤

require('core.settings')
require('core.autocmds')
require('core.utils').load_mappings()

require('core.lazy')

require('core.fixs_formats')
