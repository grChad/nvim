{
  "module default export function": {
    "prefix": "edfc",
    "body": [
      "export default function ${TM_FILENAME_BASE} (${1}) {",
      "\treturn (${2})",
      "}"
    ],
    "description": "Creates a export default function component"
  },
  "module default export function Arrow": {
    "prefix": "edfca",
    "body": [
      "const ${TM_FILENAME_BASE} = (${1}) => {",
      "\treturn (${2})",
      "}",
      "\nexport default ${TM_FILENAME_BASE}"
    ],
    "description": "Creates a export default function component Arrow"
  }
}
